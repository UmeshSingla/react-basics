const RestaurantCategoru = (props) => {
    return (
    <div key={categoryId}>
        {/** Header **/}
        ResCategory
        { /* Body */ }
    </div>
    );
};

export default RestaurantCategoru;